{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "declaration": true,
    "declarationMap": true
    // "emitDeclarationOnly": true // use if build is produced by other builders (not tsc) to avoud duplication
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
