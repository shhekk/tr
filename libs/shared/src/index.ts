export const add = (...num: number[]) => {
  return num.reduce((a, c) => (c += a));
  // return JSON.stringify(num);
};

export const multiply = (...num: number[]) => {
  return num.reduce((a, c) => (c += a));
};

export const multiply2 = (...num: number[]) => {
  return num.reduce((a, c) => (c += a));
};
