{
  "name": "@tr/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nx next:dev -p 3000",
    "serve": "nx next:dev -p 3000",
    "build": "nx next:build",
    "start": "nx next:start",
    "lint": "eslint",
    "tsc": "tsc --noEmit && echo \"reports type checking without emiting outputs\""
  }
}
